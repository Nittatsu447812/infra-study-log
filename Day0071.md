# Day71 - AWS基礎理解

## 今日やったこと

- Udemyの模擬試験を苦手分野を中心に解いて、弱点克服に努める

## 学んだこと

- ストレージクラス分析（S3 Storage Class Analysis）は、オブジェクトごとのアクセス傾向を分析する機能で、「適切なストレージクラスに移行するタイミングを見極めるために使う」
- リクエスタ支払い（S3 Requester Pays）は、オブジェクトをダウンロードした側が料金を支払う仕組み
- S3 Transfer Accelarationは、世界中のエッジロケーションを使ってS3への転送を高速化する仕組み
- マルチパートアップロード（S3 Multipart Upload）は、ファイルを分割して並列アップロードする仕組み
- Cache-Controlは、「このデータをどれくらい使っていいか」を指定するオリジン側の設定
- CloudFron利用時、よく使われるオブジェクトなのにキャッシュされていない場合、原因としてmax-ageが極端に短いか、もしくは0に設定されている可能性がある
- CloudFrontのキャッシュの決まり方（優先順位）は、①オリジンのCache-Control、②CloudFrontの設定の順に評価される（オリジン側がキャッシュするなと言えばそれに従う）
- ETagはファイルの指紋（ハッシュ値）で、前回アクセス時のときと同じなら変更なし、違うなら変更あり（CloudFrontが再取得＆再キャッシュするかの判断に使う）
- Last-Modifiedは最後に更新された日時（CloudFrontが再取得＆再キャッシュするかの判断に使う）
- AWSは基本的にマルチキャスト非対応（ユニキャストが基本）
- 例外としてTransit GatewayのMulticast機能を使うとマルチキャストをAWSでも扱える
- VPCエンドポイントは、VPCからAWSへ"インターネットを使わずに"アクセスするために使う
- SMBは、Windowsのファイル共有プロトコル
- AWS DataSyncはデータ移行ツールで、オンプレからAWSへの高速データ転送が可能
- Kubernetesの基本構造として、複数のPod（コンテナの集まり）が１つのノード（EC2）を共有する
- 特定Podしかそのノードに配置できない（ノード専用）ようにするには、Taintでノードの完全分離する（リソースを共有しない）
- 位置情報ルーティングは国・地域で固定ルーティング
- 地理的近接性ルーティングは地理的距離で最適ルーティング
- Amazon Pollyは、テキストから音声に変換する（TTS=Text To Speech）サービス
- Amazon Transcribeは、音声からテキストに変換する（STT=Speech To Text）サービス
- Amazon Lexは、会話（対話）を理解するAI（チャットボット）
- S3イベント通知は、バケット内のイベント発生をトリガーにして、SNS/SQS/Lambdaに通知設定が可能で、それと連動したシームレスな連携処理が可能
- ファイルゲートウェイは、S3を "ファイルサーバ（共有フォルダ）っぽく使える"仕組み

## 感想

認証系の問題とRoute53の問題と自動化に関する問題を中心に点を落としていることが、問題演習の結果から分かったので、そのあたりを重点的にやってみた。
本番で初めて見るサービスや用語があることを考えると、はっきりしている弱点は克服していかないと合格点に届かない。
全分野の得点率が最低でも７割以上で安定することが１つの基準になる。

## 明日やること

- Udemyで模擬試験を解きながら、弱点分野の克服に努める
