# Day133 - IT基礎理解

## 今日やったこと

- 基本情報の学習（単方向リスト）

## 学んだこと

- 単方向リストは、「データ」と「次の要素の場所」をセットで管理するデータ構造
- 配列の行番号順に並ぶのではなく、nextポインタをたどった順番がリストの順番になる
- nextが0の場合は、「次の要素がない」「リストの終端」という意味
- A[i,2]のような形でnextを表す場合、A[1,2]=3なら「1番の次は3番」という意味
- 単方向リストの問題では、値そのものよりも、まずnext列を見る
- リストを読むときは、先頭からnextをたどって「1 → 3 → 2 → 0」のように矢印で書くと分かりやすい
- A[i,2]は「iの次」を表す
- A[A[i,2],2]は「iの次の次」を表す
- A[A[A[i,2],2],2]は「iの次の次の次」を表す
- A[A[i,2],2]のような式は、必ず内側から順番に読む
- preはpreviousの略で、「前の要素」「直前の要素」を表すことが多い
- newは「新しく挿入する要素」を表すことが多い
- nextは「次の要素」または「次の要素番号を一時的に保存する変数」として使われることが多い
- followは「preの後ろに続いている要素」や「現在見ている要素の次」を表すことが多い
- 変数名は予約語ではなく、問題作成者が付けた名前なので、問題文の説明を必ず確認する
- pre、follow、newが出てきたら、「pre → follow」の間にnewを入れるのか、followを削除するのかを考える
- 挿入の基本形は「pre → follow」を「pre → new → follow」に変えること
- 挿入では、先にnewのnextを元々の後ろの要素に向ける
- 挿入の基本操作は、A[new,2] ← A[pre,2]、A[pre,2] ← new
- followという変数がある場合、挿入の基本操作は、A[new,2] ← follow、A[pre,2] ← new
- 挿入では、A[pre,2] ← newを先に実行すると、元々preの次だった要素が分からなくなることがある
- 削除の基本形は「pre → follow → next」を「pre → next」に変えること
- 削除では、削除対象そのものを消すというより、前の要素のnextを、削除対象の次へ飛ばす
- 削除の基本操作は、A[pre,2] ← A[follow,2]
- followがA[pre,2]で表される場合、preの次を削除する操作は、A[pre,2] ← A[A[pre,2],2]
- 単方向リストでは、削除対象の前の要素preが重要になる
- 単方向リストは前方向にしかたどれないので、削除したい要素だけ分かっても、その前の要素が分からないとつなぎ替えにくい
- 「pの後ろにnewを挿入」は、A[new,2] ← A[p,2]、A[p,2] ← new
- 「pの次の要素を削除」は、A[p,2] ← A[A[p,2],2]
- 単方向リストの問題では、まず現在の並びを書く
- 次に、変更後の並びを書く
- その後、どの矢印が変わるかだけを見る
- 単方向リストの正体は、矢印の付け替え問題
- 配列番号そのものに惑わされず、「誰の次が誰か」を見る
- 代入文は上から順番に実行されるので、代入順序に注意する
- 先にnextを書き換えると、後で参照したかった元のnextが失われる場合がある
- A[pre,2] ← newは、「preの次をnewにする」という意味
- A[new,2] ← followは、「newの次をfollowにする」という意味
- A[pre,2] ← A[follow,2]は、「preの次をfollowの次にする」という意味
- A[pre,2] ← A[A[pre,2],2]は、「preの次を、preの次の次にする」という意味
- new、pre、next、followのような変数名が出てきたら、日本語に置き換えると分かりやすい
- newは「新入り」
- preは「前の人」
- followは「後ろの人」
- nextは「次の人」
- 挿入は「前の人 → 新入り → 後ろの人」にする操作
- 削除は「前の人 → 消したい人 → 次の人」を「前の人 → 次の人」にする操作
- 単方向リスト問題では、頭の中だけで追わず、簡単な矢印図を書くとミスが減る

## 感想

今日は科目Bの苦手分野である単方向リストを重点的にやった。
苦手意識は少し薄れた気がする。

## 明日やること

- 基本情報の学習
