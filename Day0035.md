# Day35 - Linux基礎理解

## 今日やったこと

- 102試験の範囲の学習(1周目)

## 詰まったこと

- xinetdとsystemdの関係(socket activationの理解)
- SSHの鍵認証の流れ(公開鍵と秘密鍵の役割の整理)

## 学んだこと

- セキュリティの基本は「不要なサービスを停止し、最小限のみ公開する」こと
- スーパーサーバ(xinetd)は必要なときだけサービスを起動する仕組みで、リソース節約とセキュリティ向上に寄与する
- スタンドアローン型サービスは常時起動し即応性が高いが、リソース消費が大きい
- xinetdは「extended internet daemon」の略で、高機能なスーパーサーバ
- /etc/xinetd.conf は全体設定、/etc/xinetd.d/ はサービスごとの設定ファイルを配置するディレクトリ
- systemdでは.socketと.serviceによってxinetdの機能を代替する（socket activation）
- ListenStream は待ち受けポート、Accept=yes は接続ごとにプロセス生成を意味する
- TCP Wrapperは /etc/hosts.allow と /etc/hosts.deny によりアクセス制御を行う(allowが先に評価される)
- ポート確認には netstat(network statistics)、ss(socket statistics)を使用する
- lsof(list open files)やfuserによりポートを使用しているプロセスを特定可能
- nmap(network mapper)によるポートスキャンで外部からの公開ポートを確認できる
- SUIDは実行時に所有者権限で動作するため、権限昇格のリスクがある
- chage(change age)によりパスワード有効期限を管理し、セキュリティを強化できる
- /etc/nologin により一般ユーザーのログインを一時的に禁止可能
- su(substitute user)はユーザー切替だが監査性が低くセキュリティ的に弱い
- sudo(superuser do)はコマンド単位で権限付与でき、ログも残るため安全性が高い
- visudoは/etc/sudoers を安全に編集するためのコマンド(構文チェックあり)
- /etc/sudoersではユーザーやグループごとに実行可能コマンドを制御する
- ulimitによりプロセス数やファイル数を制限し、DoS攻撃対策が可能
- SSH(Secure Shell)は暗号化されたリモートログインを実現する
- OpenSSHはSSHの代表的実装で、sshd(サーバ)とssh(クライアント)で構成される
- 公開鍵暗号方式は公開鍵と秘密鍵のペアを用いた安全な通信方式
- RSAは現在も主流、DSAは非推奨、RSA1は廃止
- ホスト鍵はサーバの正当性を確認するために使用される(なりすまし防止)
- /etc/ssh/sshd_config によりSSHの動作やセキュリティ設定を制御する
- PermitRootLogin noやPasswordAuthentication noによりセキュリティを強化できる
- sshコマンドでリモート接続し、公開鍵認証によりパスワード不要ログインが可能

## 感想

セキュリティ分野は覚えることが多い。言葉の定義に合わせて仕組みや構造理解も必要になるので、何度も読み直す必要がある。
明日でとりあえず102試験の範囲の学習の1周目が終わる。その後は問題演習を織り交ぜながら学習を進めることになる。

## 明日やること

- 102試験の範囲の学習(1周目)
