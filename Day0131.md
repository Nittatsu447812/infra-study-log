# Day131 - Java基礎理解 & WSL基礎理解 & JavaScript基礎理解 & IT基礎理解

## 今日やったこと

### < 訓練校 >

- インタフェース
- 複数のインタフェースの実装
- WSLとUbuntu
- JavaScripptとは
- アルゴリズムと疑似言語

### < 自習 >

- 基本情報の学習

## 学んだこと

### < 訓練校 >

- スーパークラスは1つだけ（多重継承できない）
- **インターフェース**
  - クラスが持つべきメソッドを記したルールブック
  - 「そのルールブックに記されたメソッドを持っているよ」と宣言する（「インタフェースを実装する」という）ことで、継承関係にないクラス間でポリモーフィズムを使用できる
- `window.alert('はじめてのJavaScrippt!');`
  - `window` →　**オブジェクト**（動作させる対象）
  - `alert('はじめてのJavaScrippt!')` →　**メソッド**（方法、動作・命令）
  - `('はじめてのJavaScrippt!')` →　**パラメータ**（調整値）
- **キャメルケース（camelCase）**：複数の単語を繋げて書き、2語目以降の先頭を大文字にする命名ルール

### < 自習 >

- **ページイン**：必要なページを補助記憶装置から主記憶装置へ読み込むこと
- **エンディアン**：複数バイトのデータをメモリに並べるときのバイト順の決め方
- **リトルエンディアン**：下位バイトから順に、メモリの小さい番地へ格納する方式
- **シングルチップマイコン**：CPU、メモリ、入出力制御回路などを1つのICチップにまとめた小型コンピュータ
- **ヒューマンインタフェース**：人間とコンピュータ・機械が情報をやり取りするための接点や操作手段
- **アンチエイリアシング**：画像や文字のギザギザを目立たなくするために、境界部分の色をなめらかに補正する処理
- **テクスチャマッピング**：3次元CGの物体表面に、画像や模様を貼り付ける処理
- **ブレンディング**：複数の画像や色を混ぜ合わせて表示する処理。半透明表現などに使われる
- **レイトレーシング**：光の進み方を追跡して、反射・屈折・影などをリアルに描画する方法
- **ストライピング**：複数のディスクにデータを分散して書き込み、読み書きを高速化する方式
- **スワッピング**：主記憶上のプログラムやデータを、一時的に補助記憶装置へ退避したり戻したりすること
- **ディスクキャッシュ**：ディスクから読み書きするデータを主記憶上に一時保存し、ディスクアクセスを減らして高速化する仕組み
- **ミラーリング**：同じデータを複数のディスクに同時に書き込み、片方が故障しても処理を続けられるようにする方式
- **インデックス（index）**：データを高速に検索するための索引。データベースでは、表の特定の列に対して作る検索用の仕組み
- **SSID**：無線LANのネットワーク名。Wi-Fi一覧に表示される名前
- **BSSID**：無線LANアクセスポイントを識別するためのID。通常はアクセスポイントのMACアドレス
- **ゲートウェイ**：異なるネットワークや異なる通信方式の間を接続し、通信できるようにする装置・機能
- **ブリッジ**：同じ種類のネットワーク同士を接続し、MACアドレスを見てフレームを中継する装置（データリンク層）
- **ルータ**：異なるネットワーク同士を接続し、IPアドレスを見てパケットの経路を選ぶ装置（ネットワーク層）
- **リピータ**：弱くなった信号を増幅・再生して、通信距離を延ばす装置（物理層）
- **AES**：Advanced Encryption Standard の略。共通鍵暗号方式の代表例で、同じ鍵を使って暗号化と復号を行う
- **RSA**：公開鍵暗号方式の代表例。公開鍵で暗号化し、秘密鍵で復号する。素因数分解の困難さを利用している
- **楕円曲線暗号**：公開鍵暗号方式の一種。楕円曲線上の計算の困難さを利用し、RSAより短い鍵長で同程度の安全性を得やすい
- **パスワードリスト攻撃**：他サービスなどから流出したID・パスワードの組合せリストを使って、不正ログインを試みる攻撃
- **ブルートフォース攻撃**：考えられるパスワードを総当たりで試す攻撃
- **リバースブルートフォース攻撃**：1つ、または少数のよく使われるパスワードを固定し、多数のIDに対してログインを試す攻撃
- **レインボー攻撃**：あらかじめ作成された「パスワードとハッシュ値の対応表」を使って、ハッシュ値から元のパスワードを推測する攻撃
- **パスワードリスト攻撃**：他サービスなどから流出したID・パスワードの組合せリストを使って、不正ログインを試みる攻撃
- **ブルートフォース攻撃**：考えられるパスワードを総当たりで試す攻撃
- **リバースブルートフォース攻撃**：1つ、または少数のよく使われるパスワードを固定し、多数のIDに対してログインを試す攻撃
- **レインボー攻撃**：あらかじめ作成された「パスワードとハッシュ値の対応表」を使って、ハッシュ値から元のパスワードを推測する攻撃
- **アクティビティ図**：処理の流れや業務の手順を表す図。開始から終了まで、どの順番で処理が進むかを示す
- **コンポーネント図**：システムを構成する部品同士の関係を表す図。プログラム部品、ライブラリ、モジュールなどの依存関係を示す
- **シーケンス図**：オブジェクトやシステム間のメッセージのやり取りを、時間の流れに沿って表す図
- **状態遷移図**：ある対象の状態が、イベントによってどのように変化するかを表す図

## 感想

科目Aは相変わらず7~8割の正答率で安定している。
科目Bは過去にやったことのある問題を解いているので、実力を測りかねる。
連結リストが少し苦手な気がする。

## 明日やること

- 基本情報の学習
