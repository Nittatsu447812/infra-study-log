# Day79 - IT基礎理解 & Java基礎理解 & AWS基礎理解

## 今日やったこと

### < 訓練校 >

- 誤差の理解
- シフト演算の理解
- ローカル変数（局所変数）

### < 自習 >

- UdemyとPing-tで問題演習を回して、AWSの理解を深める

## 学んだこと

### < 訓練校 >

- 桁あふれ誤差は、演算結果がコンピュータの表現できる範囲を超えることで発生する誤差（オーバーフロー/アンダーフロー）
- 丸め誤差は、指定された桁数で演算結果を表すために、「切り捨て、切り上げ、四捨五入」などを行うことで発生する誤差
- 桁落ち誤差は、絶対値がほぼ等しい数値の間で、同符号の減算や異符号の加算をしたときに、有効数字が減ることで発生する誤差
- 情報落ち誤差は、絶対値の差が非常に大きい数値の間で加減算を行ったときに、絶対値の小さい数値が計算結果に反映されないことで発生する誤差
- 論理シフトは、符号を考慮しないシフト演算で、左シフト・右シフトともにあふれたビットは捨てられ、空いたビットには0がはいる
- 算術シフトは、符号を考慮するシフト演算
- 算術左シフトでは、符号ビットはそのままの位置に、あふれたビットは捨てられ、空いたビットには0が入る
- 算術右シフトでは、符号ビットはそのままの位置に、あふれたビットは捨てられ、空いたビットには符号ビットと同じビットが入る
- Java言語は幾重にもネストした多数のブロックで構成され、変数のスコープ（有効範囲）に注意を払う必要がある
- 内側のブロックで宣言された変数は参照できず、外側のブロックで宣言された変数は参照できる
- 

### < 自習 >

- マイクロサービスとは、機能ごとに小さいサービスに分割する設計をとるサービスのこと
- gRPCは、高速なサービス間通信プロトコル（マイクロサービス構成でよく使われる）
- サービスディスカバリー（サービス検出）とは、動的に変化する各サービスの接続先（IPアドレスやポート番号）をサービス同士が自動で見つけ出す仕組み
- VPC Latticeはマイクロサービス通信を簡単にするサービス
- ALBは見た目が1つでも内部では複数ノードで構成され、負荷に応じて自動スケールするため、固定ＩＰ前提ではなく、セキュリティグループ参照で制御する
- IAM Permissions Boundaryは、「この範囲以上は絶対にダメ」という上限を決める仕組み（権限のガードレール）
- AWS Control Towerには、ドリフト検出機能が最初から備わっている
- ドリフトとは、「本来あるべき状態からズレた」こと
- Transit Gatewayは、VPCの中央ルータのような存在で、VPC間の接続をスケーラブルな設計にできる
- 各VPCはTransit Gatewayへ1本接続するだけで、Transit Gatewayを経由する形で全てのVPCが相互通信が可能になる（TGWから放射線状に線が伸びて各VPCとつながっているイメージ）
- AWS PrivateLinkは、VPC全体を接続するのではなく、「特定サービスだけを安全に公開する」仕組み
- Organizationsが管理する単位は「AWSアカウント」
- Organizationsでは、特定サービスの管理を別AWSアカウントへ委譲でき、その委譲されたAWSアカウントは委任管理者（Delegated Administrator）と呼ばれる
- 委任管理者に指定できるのはAWSアカウントであり、IAMユーザーではない
- LDAP単体は、SAMLやOIDCなどのフェデレーションプロトコルに非対応な場合があり、その場合は「IAM Identity CenterによるSSO連携は直接実施できない」
- SAML非対応LDAPの場合、LDAP認証結果をAWS認証へ変換するために「カスタムIDブローカー」を利用する

## 感想

サービスとその役割自体は頭の中である程度結びついているので、問題文にその役割が記載されており、選択肢にそのサービス名があれば、選択肢を絞り込むことはできる。
しかし、選択肢の中に同じサービスの組み合わせを使っているが、その設定方法や構造が微妙に違うものが並んでいた場合に正解できていないことが多い。
何と何を組み合わせるのかということはわかっているが、それを組み合わせる方法や設定方法がまだ理解しきれていない。
2択までは絞れているのに間違ってしまうのがとてももったいない。このフェーズを抜けることができれば、大きく得点を伸ばせる気がする。

## 明日やること

- UdemyとPing-tで問題演習を回して、AWSの理解を深める
