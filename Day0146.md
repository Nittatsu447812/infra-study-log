# Day146 - PHP基礎理解 & Java基礎理解 & IT基礎理解 & JavaScript基礎理解

## 今日やったこと

### < 訓練校 >

- よく使う処理を関数化する（PHP）
- getメソッドとpostメソッド
- Java Bronzeの問題演習
- ジョブ管理とタスク管理
- insertAdjacentHTML()
- JavaScriptにおける配列、for文

### < 自習 >

- 就活準備

## 学んだこと

### < 訓練校 >

- **関数化**：繰り返し使う処理を名前付きのまとまりとして定義し、必要な場所から呼び出せるようにすること

 > [!NOTE]
 > `記述例①`：<br>
  function 関数名()<br>
{<br>
    実行する処理;<br>
}

  > [!NOTE]
  > `記述例②`：<br>
  function showMessage()<br>
{<br>
    echo "こんにちは";<br>
}<br><br>
showMessage();<br><br>
→`実行結果`：こんにちは

- **GETメソッド**：フォームの入力データをURLの末尾に付けて送信する方法で、検索や絞り込みなどの情報取得に向いている
- **POSTメソッド**：フォームの入力データをHTTPリクエストの本文に入れて送信する方法で、登録や更新、ログインなどの処理に向いている

- **ジョブ（Job）**：利用者がコンピュータに実行を依頼する処理全体の単位で、複数のプログラムやデータを含むことがある
- **タスク（Task）／プロセス（Process）**：実行中のプログラムを、使用するメモリやCPUの状態などと合わせてOSが管理する処理単位
- **ラウンドロビン方式（Round Robin Scheduling）**：実行可能な各タスクへ一定時間ずつ順番にCPUを割り当て、時間を使い切ると待ち行列の末尾へ移すスケジューリング方式
- **マルチタスク（Multitasking）**：CPUの処理対象を短時間で切り替えることで、複数のタスクを見かけ上同時に実行する仕組み
- **マルチプログラミング（Multiprogramming）**：複数のプログラムを主記憶に置き、あるプログラムが入出力待ちの間に別のプログラムを実行してCPUの稼働率を高める仕組み

- **insertAdjacentHTML()メソッド**：指定したHTML要素の直前・直後・先頭・末尾のいずれかへ、文字列として用意したHTMLを解析して追加するJavaScriptのメソッド

 > [!NOTE]
 > `記述例`：<br>
 要素.insertAdjacentHTML("挿入位置", "追加するHTML");

- JavaScriptの配列の記述例：
 > [!NOTE]
 > const fruits = [<br>
 "りんご",<br>
 "みかん",<br>
 "ぶどう"<br>
 ];

- **インデックス（Index）**：配列内の各要素に付けられる0から始まる位置番号

 - 配列の値を変更する：
 > [!NOTE]
 > const fruits = ["りんご", "みかん", "ぶどう"];<br><br>
fruits[1] = "バナナ";<br><br>
console.log(fruits);<br><br>
→`実行結果`：["りんご", "バナナ", "ぶどう"]

## 感想

転職サイトのキャリア面談を受けた。
そこで話した希望内容に合わせて、キャリアアドバイザーの方から多くの非公開求人を紹介してもらった。
私の希望に沿うもの、沿わないものが入り混じっているが、魅力的に思える求人も数個あった。

## 明日やること

- 就活準備
