# Day115 - Java基礎理解 & IT基礎理解

## 今日やったこと

### < 訓練校 >

- クラス変数とクラスメソッド
- クラス変数の初期化とインスタンス変数の初期化
- staticキーワードに関する問題演習
- データの正規化
- トランザクション処理

### < 自習 >

- 基本情報の学習
- Javaの総復習

## 学んだこと

### < 訓練校 >

- **クラス変数**
  - staticのついたフィールドで
  - コンパイル後、実行前には使えるようになっている
  - 宣言と同時に初期化される
- **インスタンス変数**
  - staticのついていないフィールド
  - クラスからインスタンスを作ってからでないと使えない
  - インスタンスを作るタイミングでコンストラクタにより初期化される
  - **クラスメソッドから呼び出せない**
- **データの正規化**：データの重複や更新時の不整合を減らすために、表を適切に分割・整理すること
- **非正規形**：1つの項目に複数の値が入っているなど、表として整理されていない状態
- **第1正規形**：1つのマスに1つの値だけが入るようにした状態（繰返し項目を排除し、また計算で求められる項目も削除する）
- **複合主キー**：複数の属性を組み合わせて、1行を一意に識別する主キー
- **第2正規形**：第1正規形であり、複合主キーの一部だけに依存する属性を別表に分けた状態
- **部分関数従属**：複合主キーの一部だけで、ある属性の値が決まる関係
- **完全関数従属**：複合主キー全体によって、ある属性の値が決まる関係
- **第3正規形**：第2正規形であり、主キー以外の属性に依存する属性を別表に分けた状態
- **推移的関数従属**：主キーではない属性を経由して、別の属性の値が決まる関係
- **トランザクション処理**：複数の処理を1つのまとまりとして扱い、すべて成功なら確定し、途中で失敗したら元に戻す処理
- **ACID特性**：トランザクション処理で守るべき4つの性質。原子性・一貫性・独立性・耐久性のこと
  - **原子性（Atomicity）**：トランザクションは全部実行されるか、全部取り消されるかのどちらか
  - **一貫性（Consistency）**：処理の前後でデータの整合性が保たれる
  - **独立性（Isolation）**：複数のトランザクションを同時に実行しても、互いに悪影響を与えない
  - **耐久性（Durability）**：確定した結果は、障害が起きても失われない
- **排他制御**：複数の処理が同じデータを同時に更新して矛盾が起きないように制御すること
- **ロック**：他の処理が同じデータを勝手に更新・参照できないように、一時的に使用制限をかけること
- 行単位ロック
  - 細かい
  - 他の人が別の行を操作しやすい
  - 同時実行性が高い
- 表単位ロック
  - 大きい
  - 他の人が待たされやすい
  - 同時実行性が低い
- **共有ロック**：他の処理も読むことはできるが、更新はできないロック。主に参照時に使う
- **専有ロック**：他の処理が読むことも更新することも制限するロック。主に更新時に使う
- **ロックの粒度**：ロックをかける範囲の大きさ。表全体・ページ単位・行単位などがある
- **デッドロック**：複数の処理が互いに相手のロック解除を待ち続け、処理が進まなくなる状態
- **2相コミットメント**：分散データベースなどで、複数のシステムにまたがる更新を全体で確定するための手順
- **アクセス透過性**：利用者がデータの物理的な場所や構成を意識せず、同じようにアクセスできる性質
- **Hadoop**：大量のデータを複数のコンピュータに分散して保存・処理するためのオープンソースの基盤

### < 自習 >

- 配列(Array)は同じ型のデータを複数まとめて扱う仕組み
- int[] nums = {10,20,30}; のように宣言する
- 配列の要素は添字（インデックス）で指定する
- **Javaの添字は0から始まる**
- nums[0]は最初の要素、nums[1]は2番目の要素、nums[2]は3番目の要素
- 配列の要素は nums[1] = 50; のように変更できる
- **メソッドは処理をまとめたもの**
- メソッドは呼び出して実行する
- sayHello(); は sayHelloメソッドを呼び出す
- 引数はメソッドに渡す値
- 仮引数は受け取る側の変数
- 実引数は渡す側の値
- sayHello("AWS"); の "AWS" は実引数
- sayHello(String name) の name は仮引数
- returnは結果を呼び出し元へ返す
- return a + b; は a+b の結果を返す
- **オブジェクト指向**
  - クラス(class)は設計図
  - インスタンス(instance)は設計図から作られた実物
  - new Person() は Personクラスのインスタンスを作成する
- Person p = new Person();
  - Person → クラス（設計図）
  - new Person() → インスタンス生成
  - p → インスタンスを扱うための変数
- **フィールド（インスタンス変数）**：インスタンスが持つデータ
  - String name; は名前を表すフィールド
  - int age; は年齢を表すフィールド
- p.name = "Taro"; → pが指すインスタンスのnameフィールドに"Taro"を代入する
- p.age = 20; → pが指すインスタンスのageフィールドに20を代入する
- **コンストラクタ**
  - コンストラクタはインスタンス生成時専用のメソッド
  - クラス名と同じ名前を持つ
- Person(String n, int a)
  - nは仮引数
  - aは仮引数
- new Person("Taro", 20);
  - "Taro"がnに入る
  - 20がaに入る
- name = n; → フィールドnameにnの値を代入する
- age = a; → フィールドageにaの値を代入する
- **this**
  - thisは「このインスタンス」を表す
  - this.name はこのインスタンスのnameフィールド
  - name は仮引数
- this.name = name;
  - フィールドnameに仮引数nameの値を代入する
- **static**
  - staticが付かない変数はインスタンス変数
  - インスタンス変数はインスタンスごとに別々に持つ
  - staticが付く変数はクラス変数
  - クラス変数は全インスタンスで共有する
- static int count; → countはクラス変数
- String name; → nameはインスタンス変数

## 感想


## 明日やること

- 基本情報の学習
- Webクリエイター試験に向けた学習
