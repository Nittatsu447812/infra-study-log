# Day86 - Webデザイン基礎理解 & Java基礎理解 & AWS基礎理解

## 今日やったこと

### < 訓練校 >

- Webサイト作成練習
- Javaの演算子と分岐文、ループ文の総復習

### < 自習 >

- UdemyとPing-tで問題演習を解いて、AWSの理解を深める

## 学んだこと

### < 訓練校 >

- `:first-child`は、「親要素から見て最初の子要素」を指し、リストの最初の要素だけ上線を消す場合などに使われる
- `:last-child`は、「親要素から見て最後の子要素」を指し、リストの最後の要素だけ下線を消す場合などに使われる
- `:nth-child(n)`は、「親要素から見て指定したn番目の子要素」を指し、表（テーブル）の偶数行だけ背景色をグレーにするといった場合に使われる
- `nth-child(odd)`は、「親要素から見て奇数番目の子要素」を指す
- `nth-child(even)`は、「親要素から見て偶数番目の子要素」を指す
- `nth-child(2n+1)`は、「親要素から見てすべての子要素のうち、1、3、5、7...番目の子要素であり、尚且つセレクタが一致する子要素」を指す（nには0から順番に入れていく）
- `nth-of-type(n)`は、「子要素のうち、同じ種類のタグ（要素名）だけを抽出して1, 2, 3...と数え、一致する子要素」を指す
- `=、+=、-=、*=、/=、%=`は、優先順位が最も低い演算子
- ダブルクォーテーション「"」で囲まれたものは、中身が数字であってもすべて文字列（String型）として扱われる
- 


### < 自習 >

- Lamdbaの呼び出し許可は、リソースベースポリシーのLamdbaに追加することで実現する
- リソースベースポリシーのPrincipalにはevents.amazonaws.comなどのLambdaを呼び出す主体となるサービスを指定し、Actionにはlambda:InvokeFunctionを指定する
- NACLは、ルール番号の小さい順に上から順番に評価され、最初に一致したルールが採用される
- SCP有効化時にデフォルトで「FullAWSAccess」が自動付与される
- SCPはAND条件で評価されるため、デフォルト設定から新たなSCPを設定する場合は、既存のSCPをデタッチする必要がある
- CloudFront FunctionsとLambda@Edgeは、両方とも「エッジロケーション上で動く処理」を担い、「どこまで高度な処理ができるか」が違う
- CloudFront Functionsは、URL書き換え、Header追加、Cookie判定、軽度リダイレクト、国別ページ分岐、A/Bテストなどの軽い前処理を行える（軽量高速）
- Lambda@Edgeは、JWT認証、認可処理、外部API連携、HTML動的加工、高度なレスポンス制御、本格的なロジックなどを行える（高機能）
- 

## 感想


## 明日やること

- UdemyとPing-tで問題演習を解いて、AWSの理解を深める
