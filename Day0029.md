# Day29 - Linux基礎理解

## 今日やったこと

- 102試験の範囲の学習(1周目)

## 詰まったこと

- ハッシュ関数の仕組み

## 学んだこと

- パスワードは、/etc/shadowで安全に管理されている
- /etc/shadowの第2フィールド(パスワード)が、!のときはパスワードがロックされていて、*のときは最初からログインさせるつもりがない(システム用ユーザーに多い)
- newgrp/useradd/usermod/userdel/passwd/groupadd/groupmod/groupdel/id/getentコマンド
- newgrpコマンドはgroupを変更し、新しいシェルを起動し、変更が即反映される
- usermod -Gはサブグループの変更(上書き)で、-aGはサブグループ追加
- userdelコマンドでユーザーアカウントを削除できるが、ホームディレクトリは残る
- ホームディレクトリも削除したい場合は、userdel -rコマンドを使う
- /etc/skelは新規ユーザー作成時の雛形
- getentはLDAPなども含めて情報を取得できる
- groupdelコマンドはグループを削除できるが、指定したグループをプライマリグループとするユーザーがいる場合は削除できない

## 感想

今日は実機に触る時間を意識的に増やして学習を進めた。おかげで設定ファイルやコマンド操作への理解は深まったと思う。
明日以降もこの感じで進めることにする。4月6日の入校日までに102試験に合格しておくのが理想的ではあるが、実機に触る時間を削ってはいけない。
資格のための学習になっては本末転倒である。資格勉強をインフラエンジニアになるための学習の指針として利用し、知識も技術も成熟させるのが目的である。
資格のための学習にならないように意識する必要がある。

## 明日やること

- 102試験の範囲の学習(1周目)
