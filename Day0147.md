# Day147 - PHP基礎理解 & IT基礎理解 & JavaScript基礎理解

## 今日やったこと

### < 訓練校 >

- 適正体重の計算アプリ（追加機能とXSS対策）
- 郵便番号検索プログラム（APIからデータを読み込む）
- 

### < 自習 >

- 就活準備

## 学んだこと

### < 訓練校 >

- **abs()関数**：PHPで指定した数値の絶対値を返し、負の数は正の数に変換して正の数と0はそのまま返す関数

> [!NOTE]
> `記述例`：echo **abs**(-4.2);<br>
> `実行結果`：4.2

- **str2html()関数**：PHPの標準関数ではなく、文字列をHTMLへ安全に出力する処理などをまとめるために独自に定義されることがある関数名

- **htmlspecialchars()関数**：HTMLで特別な意味を持つ文字を文字実体参照へ変換し、入力されたタグやスクリプトがHTMLとして解釈されるのを防ぐPHPの標準関数

- **API（Application Programming Interface）**：アプリケーション同士が、決められた方法で機能やデータをやり取りするための接続窓口
- **JSON（JavaScript Object Notation）形式**：データを「プロパティ名と値」の組み合わせで表す、軽量で人間にも読みやすいデータ記述形式

> [!NOTE]
> `JSON形式の記述例`：<br>
> {<br>
&ensp;"serverName": "web-server-01",<br>
&ensp;"ipAddress": "192.168.1.10",<br>
&ensp;"status": "running"<br>
}

- **REST（Representational State Transfer）**：URLで操作対象となるリソースを表し、GETやPOSTなどのHTTPメソッドを使って操作するWeb APIの設計方式

> [!NOTE]
> `HTTPメソッドの例`：<br>
> **GET**&emsp;/servers&emsp;→&emsp;サーバー一覧を取得<br>
> **GET**&emsp;/servers/1&emsp;→&emsp;IDが1のサーバーを取得<br>
> **POST**&emsp;/servers&emsp;→&emsp;新しいサーバーを登録<br>
> **PUT**&emsp;/servers/1&emsp;→&emsp;IDが1のサーバーを更新<br>
> **DELETE**&emsp;/servers/1&emsp;→&emsp;IDが1のサーバーを削除

> [!TIP]
> `全体の流れ`：<br>
> 利用者側のアプリ<br>
&emsp;↓ APIを呼び出す<br>
REST方式で設計されたWeb API<br>
&emsp;↓ JSON形式でデータを返す<br>
利用者側のアプリ

- **json_decode()関数**：JSON形式の文字列を、PHPで扱えるオブジェクトや配列などのデータへ変換する関数

> [!NOTE]
> `json_decode()関数の基本的な使い方`：<br><br>
> ＜次のJSON文字列をPHPのデータへ変換する＞<br><br>
> $json = '{<br>
&ensp;"name": "web-server-01",<br>
&ensp;"status": "running",<br>
&ensp;"port": 443<br>
}';<br><br>
$data = json_decode($json);<br><br>
> ＜第2引数を指定しない場合、JSONのオブジェクトはPHPのオブジェクトへ変換される＞<br><br>
> echo $data->name;&emsp;// web-server-01<br>
echo $data->status;&emsp;// running<br>
echo $data->port;&emsp;// 443<br>

> [!NOTE]
> `json_decode()関数を使って**連想配列へ変換する**`：<br><br>
> ＜第2引数に`true`を指定すると、JSONのオブジェクトをPHPの連想配列へ変換できる＞<br><br>
> $data = json_decode($json, true);<br><br>
echo $data["name"];   // web-server-01<br>
echo $data["status"]; // running<br>
echo $data["port"];   // 443


## 感想


## 明日やること

- 就活準備
