# Day89 - IT基礎理解 & Webデザイン基礎理解 & Java基礎理解 & AWS基礎理解

## 今日やったこと

### < 訓練校 >

- ベン図と論理式の理解
- CSSルールが適応される優先順位
- Windowsアプリの「ターミナル」で、コンパイル、プログラム実行の仕方
- システムの性能評価の理解

### < 自習 >

- UdemyとPing-tで問題を解いて、AWSの理解を深める

## 学んだこと

### < 訓練校 >

- CSSルールの適用優先順位は、記述場所（重要度）、詳細度（セレクタの具体性）、記述順（カスケード）の3つのルールによって厳格に決定される
- プロパティの直後に `!important`をつけると、他のあらゆるルールを無視して最優先される
- インラインスタイル（HTMLタグの style 属性に直接書き込まれたスタイル）がその次に優先される
- `<link>`タグで読み込む外部CSSや、HTMLの`<style>`タグ内に記述された通常のCSSがその次に優先される
- カスケード（Cascading）とは「複数のCSSルールが衝突したときに、特定の規則に従って上から下へ流れるようにスタイルを適用・統合する仕組み」
- `position: fixed;`は、（画面への固定）要素をブラウザの表示画面（ビューポート）に対して絶対的な位置に固定するプロパティで、画面をスクロールしても、その要素だけは動かずにその場に留まる
- `z-index`は、（重なり順のコントロール）要素の「手前・奥」の重なり順（立体的な優先度）を指定するプロパティ
- `<br>`タグとは、HTMLで「強制的に改行を入れる」ためのタグ
- `<required>`属性とは、「その入力項目が必須であること」をブラウザに伝えるHTMLの属性
- スループットは、単位時間当たりの処理される仕事の量
- ターンアラウンドタイムは、利用者が処理を依頼してから、処理を出力が終了するまでの時間
- レスポンスタイムは、利用者が処理を開始してから、処理結果が出始めるまでの時間（応答時間）
- ベンチマークテストは、システムの使用目的に合った標準的なプログラムを実行させ、その測定値から処理性能を相対的に評価する方法

### < 自習 >

- .NETは、Microsoftのアプリ開発基盤
- IISは、MicrosoftのWebサーバ（Linux系でいうApacheやNginx）
- 別AWSアカウントのリソースへ安全にアクセス → IAMロールを引き受ける → STS AssumeRole
- Multi-AZ DBクラスターは、同一リージョン内の複数AZ構成
- ACM発行証明書は自動更新ができるが、外部CA証明書はImportは可能だが更新は自分で手動で行う必要がある
- ACM（AWS Certificate Manager）は「外部CA証明書の自動更新」まではやってくれない

## 感想

Ping-tの模擬試験のモードで初見問題に絞って問題を解いてみた。57問/65問で正答率88%だった。
重箱の隅をつくような問題以外はほとんど正解できている。
しかし、作問者の意図したひっかけに見事に引っかかることもあるので、問題文を読み飛ばしたり、勝手に決めつけたりしないように注意する必要がある。

## 明日やること

- UdemyとPing-tで問題を解いて、AWSの理解を深める
