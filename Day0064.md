# Day64 - AWS基礎理解

## 今日やったこと

- Udemyの模擬試験を解いて、AWSへの理解を深める

## 学んだこと

- Amazon S3でバージョニングを有効化すると、誤って削除しても復元できるようになり、上書きしても過去データを保持できるようになる
- 証明書（SSL/TLS）は、「そのサイトが本物で、安全に通信できますよ」という身分証明書
- AWS Certificate Manager（ACM）が証明書管理を行う
- ホットリンクとは「他人のサーバの画像や動画を"直リンクで勝手に使うこと"」
- Refere（リファラ）は「このリクエストは、どのページから来たのか」を示す情報
- AWS WAFは「Webへのアクセスをルールで制御する防御装置」
- FIFOは「First in,First out（先入れ先出し）」、順序を守り、重複排除をする
- Amazon SESは、AWSのメール送信サービス
- オブジェクトロックはWORM（Write Once Read Many）、一度書いたら変更・削除できない設定
- S3のオブジェクトロックには２種類あり、コンプライアンスモードとガバナンスモードがある
- コンプライアンスモードは誰も解除できない
- ガバナンスモードは管理者なら削除できる
- オブジェクトロックを既存のS3バケットに対して有効化することはできず、バケット作成時に設定する必要がある
- 加重ルーティング（Weighted）は割合で振り分ける仕組み（例：サーバAに80%、サーバBに20%）
- マルチバリュールーティング（Maltivalue）は複数の正常なIPを返し、クライアントがその中から一つを選んで接続する仕組み
- レイテンシールーティングはユーザーから見て最速のリージョンに振り分ける仕組み
- 問題を解くときのコツとして、「ギリギリは避ける（時間や容量など）」

## 感想

だんだんとAWSのサービスとその役割や使い分けがわかってきたように思う。
問題を解くときも堪ではなく、ある程度の根拠を持って解けている自信がある。
後は読み飛ばしてきた細かい部分まで頭に入れていく必要がある。

## 明日やること

- AWSの学習を進める
