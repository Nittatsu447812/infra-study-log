# Day42 - Linux基礎理解

## 今日やったこと

- 102試験の範囲の学習(2周目)
- Ping-tで問題演習と分からなかったところの復習

## 詰まったこと

- IPv6のアドレスが/65のときのプレフィックス

## 学んだこと

- 対話的ログイン禁止とログイン禁止の違い
- 対話的ログインを禁止するには、ユーザーのデフォルトシェルを/bin/falseや/bin/nologinに設定する
- サービス用ユーザーは基本的にnologinに設定されている
- mailコマンドを対話モードで実行した場合、「.」を入力することで、メールを送信して対話を終了する(LPIC的/古い)
- 実装によっては「 Ctrl+D 」で対話モードを終了する(実務的/新しい)
- 届いたメールをmailコマンドで見るときに、既読メールを自動削除する設定になっていた場合、「q」で閲覧を終了すると、そのメールは削除される(変更保存/既読は削除)
- 届いたメールを消したくない場合は、「x」で閲覧を終了するとメールは削除されない(変更破棄/削除されない)
- System V印刷システム(System V系)では、lp(印刷)、lpstat(状態確認)、cancel(ジョブ削除)を使う
- 上位プロトコル/下位プロトコルは「あるプロトコルから見て、より上の層/下の層(アプリケーション層やトランスポート層など)で使われるプロトコル」という相対的な概念

## 感想

段々と理解は深まっている気はするが、101試験の範囲を1周したときよりも、理解度が低い状態からのスタートにはなっている。
「学校が始まるまでに資格習得」を目標にしていたため、雑な1周目になっていたんだと思う。
焦燥感を持って学習に臨むことは悪いことではないが、「資格のための勉強になってはいないか」を常に自問し学習を進めたい。

## 明日やること

- 102試験の範囲の学習(2周目)
- Ping-tで問題演習と分からなかったところの復習
