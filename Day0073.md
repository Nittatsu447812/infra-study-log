# Day73 - AWS基礎理解

## 今日やったこと

- Udemyの模擬試験を解いて、AWSの理解を深める

## 学んだこと

- AWS DMS（Database Migration Service）は、データベースを止めずに移行するサービス（異種DB間の移行も可能）
- AWS STC（Schema Conversion Tool）は、DBの設計（スキーマ）を変換するツール
- ①SCTでスキーマ変換、②DMSでデータ移行という流れ
- 「LambdaをHTTPで呼ぶ」＝API Gateway or 関数URL
- 未来の決まった日時に確実にインスタンスを起動したい＝EC2 Capacity Reservation（キャパシティ予約）
- Amazon ElastiCache for Redisは、メモリDBだが停止・再起動してもデータを永続化できる（データを残したいならRedis）
- Amazon ElastiCache for Memcachedは、完全なキャッシュ専用でデータを永続できない（消えてもOKな一時データ用）
- NASストレージは、ネットワーク越しに複数サーバから使える共有ストレージ（AWSではAmazon EFS）
- Amazon Macieは、S3の中身をスキャンして「個人情報（PII）」を見つけるサービス
- Amazon DataZoneは、社内外のアカウント間で"データ共有して分析する"ためのサービス（コピーせずに共有して分析させる）
- AWS Data Exchangeは、データを"外部に提供・販売"するためのサービス（ビジネスとしてデータを配る）
- AWS Transfer Familyは、SFTP/FTPでファイルを転送するサービス（ただのファイルコピー）
- AWS Application Migration Serviceは、サーバを"丸ごとコピーしてAWSに持ってくる"サービス

## 感想

まだ解いたことのなかった模擬試験を本番想定で解いてみたが、散々な結果だった。知らないサービスも山のように出てきて、昨日までの自信はすべて慢心であることがわかった。
SAAの取得は、5月末に間に合えば上出来、6月中旬が現実的なラインになると思う。

## 明日やること

- Udemyの模擬試験を解いて、AWSの理解を深める
