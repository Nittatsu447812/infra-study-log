# Day36 - Linux基礎理解

## 今日やったこと

- 102試験の範囲の学習(1周目)

## 学んだこと

- SSHは安全な通信のための仕組みで、ホスト認証と公開鍵認証を行う
- ホスト認証では、~/.ssh/known_hostsにサーバの公開鍵を保存し、なりすましを防ぐ
- 公開鍵認証では秘密鍵と~/.ssh/authorized_keysを使ってログイン認証を行う
- ssh-keygenコマンドで鍵ペアを作成する
- パスフレーズは秘密鍵の保護のために設定する追加パスワード
- ssh-agentは秘密鍵のパスフレーズをキャッシュする
- ssh-addコマンドでssh-agentに鍵を登録する
- scpコマンドはSSHを使った安全なファイル転送ができる
- SSHポートフォワーディングには、ローカル(-L)、リモート(-R)、ダイナミック(-D)がある
- X11ポート転送はリモートのGUIアプリをローカルに表示する仕組み
- GnuPG(GNU Privacy Guard)はファイルの暗号化や署名を行うツール
- gpgコマンドで暗号化・復号・署名・鍵管理を行う
- ~/.gnupgに鍵や設定情報が保存される
- 公開鍵は配布用、秘密鍵は自分だけが保持する(秘密鍵は絶対に漏らさない)
- 失効証明書は鍵を無効化するために使う
- 失効証明書は鍵を作成した直後に作成し、安全に保管する(USBに保管/オフライン保管/印刷して保管)
- gpg -cは共通鍵(パスワード)による暗号化
- gpg -e -rは公開鍵を使った暗号化(特定の相手向け)
- gpg -dで復号する
- gpg -sは秘密鍵で署名を行い、改ざん検知が可能
- gpg --verifyで署名検証ができる
- gpg-agentはパスフレーズ入力を省略するための仕組み

## 感想

今日で102試験の範囲の学習を始めて9日目。ようやく1周目が終わった。
まだまだ理解は浅く覚えていないことも多いが、ここから問題演習を並行しながら2周目を進めることでさらに理解を深めたい。
学校が開始する前に102試験に合格するのが理想的であったが、それは日程的に厳しそうである。
焦って不合格になると受験料がもったいないので、Ping-tで高正答率が維持できるようになるまでは受験しない予定である。
ただ最寄りの受験会場は試験日直前でも予約可能なので、条件が整えばすぐにでも受けに行く予定である。

## 明日やること

- 102試験の範囲の学習(2周目)
- Ping-tで問題演習と分からなかったところの復習
