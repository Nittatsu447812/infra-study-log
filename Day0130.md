# Day130 - PHP基礎理解 & Java基礎理解 & IT基礎理解

## 今日やったこと

### < 訓練校 >

- PHP、Laravel（ララベル）とは
- 抽象クラスと抽象メソッドの復習
- インタフェース
- アルゴリズム

### < 自習 >

- 基本情報の学習

## 学んだこと

### < 訓練校 >

- **PHP**：Webアプリケーション開発でよく使われるプログラミング言語。HTMLの中に処理を書いたり、サーバ側でデータベース処理や画面生成を行ったりする
  - 言語そのもの
  - Webページの裏側の処理を書く
- **Laravel**：PHPでWebアプリケーションを効率よく作るためのフレームワーク
  - PHP用の開発フレームワーク
  - ログイン機能、DB操作、ルーティング、画面表示などを作りやすくする
- **XAMPP**：Webアプリ開発に必要なソフトをまとめて簡単に入れられる開発環境パッケージ
  - **X**：Windows、macOS、Linuxなど複数OS対応の意味
  - **A**：Apache（Webサーバ）
  - **M**：MariaDB / MySQL（データベース）
  - **P**：PHP（サーバ側で動くプログラミング言語）
  - **P**：Perl（プログラミング言語）
- **抽象クラス**
  - 未完成のメソッドを含むことができるクラス
  - 直接インスタンス化せず、サブクラスに継承させて使う
- **抽象メソッド**
  - メソッド名・戻り値・引数だけを定義し、処理内容を書かないメソッド
  - サブクラスで具体的な処理を書く
- **インターフェース**
  - クラスに「必ず持つべきメソッド」を定めるための仕組み
  - 具体的な処理内容は書かず、実装するクラス側で中身を書く

### < 自習 >

- 16進数から2進数への変換：16進数の1桁を、2進数の4桁に置き換える
- **フィードバック制御**：出力結果を確認し、目標値との差を見ながら制御する方式
- **フィードフォワード制御**：結果が出る前に、外乱や変化を予測して先に制御する方式
- **値呼出し**
  - 変数の「値のコピー」を渡す
  - サブルーチン内で変更しても、元の変数は変わらない
- **参照呼出し**
  - 変数そのものの場所、つまり参照を渡す
  - サブルーチン内で変更すると、元の変数も変わる
- **結果呼出し**
  - サブルーチン終了時に結果を呼び出し元へ戻す
  - 元の変数が変わる可能性がある
- **名前呼出し**
  - 引数名を式のように扱う特殊な渡し方
  - 元の変数に影響する可能性がある
- **インデックス修飾**：命令のアドレス部に、インデックスレジスタの値を加えて実効アドレスを求める方式
- **シリアルATA**：PCとHDD・SSD・光学ドライブなどを接続するためのインターフェース規格。SATAとも呼ばれ、データを1ビットずつ順番に送るシリアル転送を使う
- **ポートマルチプライヤ**：1つのSATAポートに、複数のSATA機器を接続できるようにする仕組み
- **フォールトトレラントシステム**：一部に故障が発生しても、システム全体の処理を継続できるようにしたシステム
- **シンプレックスシステム**：1台の処理装置だけで構成されるシステムで、構成が単純でコストは低いが、故障するとシステムが停止する
- **デュアルシステム**：2台の処理装置で同じ処理を同時に行い、結果を照合するシステム。信頼性が高い
- **パラレルプロセッサシステム**：複数のプロセッサで処理を分担し、並列に処理するシステム。処理性能を高めることが目的
- **デュプレックスシステム**：2台の処理装置を用意し、通常は片方を稼働系、もう片方を待機系として運用するシステムで、稼働系が故障したら、待機系に切り替えて処理を継続する

## 明日やること

- 基本情報の学習
