# Day75 - Excel基礎理解 & Java基礎理解 & AWS基礎理解

## 今日やったこと

### < 訓練校 >

- Excelの表計算
- Javaにおける処理の繰り返し（for文、while文、do~while文）
- 

### < 自習 >

- Udemyで模擬試験を解いて、AWSの理解を深める

## 学んだこと

### < 訓練校 >

- コピー元が一番上のセルになるように、コピー先を含めて範囲指定をして`Ctrl`+`D`を押すことで、一番上のセルの内容が範囲指定したセルにコピーされる
- コピー元が一番左のセルになるように、コピー先を含めて範囲指定をして`Ctrl`+`R`を押すことで、一番左のセルの内容が範囲指定したセルにコピーされる
- `$`は固定を表す記号（例：B$4は4行目固定を表す）
- `$`を使わずに数式などを記述した場合、数式内のそれぞれのセルの相対位置に当てはまるセルの値が使用される
- ORは論理和、ANDは論理積を表す
- COUNTは個数（セルが何個か）、COUNTIFは条件付個数（条件に当てはまるセルの個数）を表す
- SUMは合計（指定した範囲のセルの値の合計）を表す
- IFは条件分岐（IF(条件式),"trueの場合に表示するもの","falseの場合に表示するもの"という書式）
- 決まった回数だけ処理を繰り返し実行したいとき、for文を使う
- for (最初の処理; 条件式; 命令文の実行後に行う処理) { 命令文 }という構文である
- while文はfor文と同様の目的で使われるが、記述の仕方（構文）が異なる
- while (条件式) { 命令文 }という構文である
- do~while文は、while文と似ているが、命令文を実行してから条件式が評価されるという点が異なる
- do~while文は、必ず1回はブロック内の命令文が実行され、その後で条件式が評価される

### < 自習 >

- Auto Scalingの3つの基本値はMinimum capacity（最小）、Maximum capacity（最大）、Disired capacity（希望）
- CloudFrontにおけるキャッシュキーは、「リクエストを一意に識別するための複数要素の組み合わせ」（URLパス、クエリ文字列、HTTPヘッダー、Cookie）
- その複数要素をまとめてハッシュ化することで一致判定している
- クエリ文字列の扱いを制御してキャッシュキーの分岐を減らすことで、キャッシュヒット率を上げることができる
- DynamoDB グローバルテーブル（Global Tables）は、クロスリージョンレプリケーション機能であり、利用するにはDynamoDB Streamsを有効化する必要がある
- カスタマーゲートウェイは、VPN接続におけるオンプレ側の入口であり、ルーターやFWに設定される
- 仮想プライベートゲートウェイは、VPN接続におけるVPC（AWS）側の入口である
- AWS Network Firewallは、VPCの通信を検査・制御する「本物のファイアウォール」
- AWS Firewall Managerは、複数アカウント・複数VPCのファイアウォールを一括管理するサービス（セキュリティ設定の司令塔）
- インスタンスプロファイルは、EC2専用の「ロールの入れ物」
- インスタンスメタデータは「EC2インスタンス自身の情報を取得するための"内部API"」

## 感想

とにかく問題演習を回して、精度を上げていくフェーズに入った。未知のサービスが出てくることは減ってきているが、知っているサービスの知らない仕様が問題として出てくることはよくある。問題演習をこなしながら詰めていきたい。

## 明日やること

- Udemyの模擬試験を解いて、AWSの理解を深める
