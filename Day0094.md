# Day94 - IT基礎理解 & Java基礎理解 & AWS実機操作

## 今日やったこと

### < 訓練校 >

- 論理演算の問題演習
- メソッドの戻り値
- メソッドのオーバーロード

### < 自習 >

-　AWS実機操作

## 学んだこと

### < 訓練校 >

- 特定のビットを反転させるには「排他的論理和（EOR）」を使用する
- 反転位置を「1」、不変位置を「0」にしたビット列（マスクデータ）とEOR演算を行う
- 戻り値とは、メソッドから返される値のこと
- `void`は、メソッドに戻り値がない場合に、戻り値の型として使用するキーワード
- `return`は、戻り値のあるメソッドで、戻り値を指定するのに使用するキーワード
- オーバーロードとは、引数が異なる、同じ名前のメソッドを宣言すること
- オーバーロードの条件
　　- 引数の型が違う
  　- 引数の数が違う
    - 引数（複数ある場合）の順番が違う
  - いずれかの場合、メソッドをオーバーロードする
  - これらの「メソッドを個別に識別するための名前と引数の組み合わせ」のことを、メソッドの「シグニチャ（Signature）」と呼ぶ


### < 自習 >

- S3を直接パブリック公開しなくても、OACを利用することで、CloudFront経由で配信できる
- CloudFrontはコンテンツをキャッシュするため、S3更新が即時反映されないことがある
- CloudFront Functionsは軽量な認証処理に適している
- Viewer Requestで認証処理を行うことで、S3到達前にアクセス制御ができる

## 感想

S3とCloudFrontを利用してWebページを公開し、さらにCloudFront Functionsを使ってBasic認証を実装し、Webページを限定公開することができた。
Route53を使ったドメイン名の設定やWAFを使ったIP制限やGeo Matchなど、追加で実装したい機能がたくさんある。実機操作を学びながら少しずつ機能を足していきたい。

## 明日やること

- AWS実機操作
