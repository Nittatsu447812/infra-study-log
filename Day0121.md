# Day121 - Java基礎理解 & Webデザイン基礎理解 & IT基礎理解

## 今日やったこと

### < 訓練校 >

- フィールドとメソッドの継承
- 継承関係とコンストラクタの動き（**デフォルトコンストラクタ**）
- モジュール分割
- 

### < 自習 >

- 基本情報の学習
- Webクリエイター試験に向けた学習

## 学んだこと

### < 訓練校 >

- 子クラスのメソッドから親クラスのメソッドを呼び出したいとき、`super`を使う
- コンストラクタとは、クラス（設計図）からインスタンスを生成するための初期化メソッド
- - **デフォルトコンストラクタ**：クラスにコンストラクタを1つも書かなかった場合に、コンパイラが自動で用意する引数なしのコンストラクタ
  - コンストラクタを書かない → デフォルトコンストラクタが自動生成される
  - コンストラクタを1つでも書く → デフォルトコンストラクタは自動生成されない
  - デフォルトコンストラクタは引数なし → `クラス名()`
  - デフォルトコンストラクタの中では、暗黙的に `super()` が呼ばれる
- **デザインの4つの基本原則**：近接・整列・反復・コントラストの4つ。見やすく分かりやすいレイアウトを作るための基本原則
- **近接**：関連する情報を近くに配置し、関係のあるまとまりとして見せること
- **整列**：文字や画像の位置をそろえて、画面全体に統一感を出すこと
- **反復**：同じ色・形・余白・見出しスタイルなどを繰り返し使い、統一感を出すこと
- **コントラスト**：大きさ・色・太さ・明暗などに差をつけ、重要な部分を目立たせること
- **カーミング**：落ち着き・安心感・穏やかさを感じさせるデザイン表現。淡い色、余白、柔らかい形などを使うことが多い
- **大和比**：1：√2 の比率。A判・B判の紙の縦横比に使われる
- **シンメトリー**：左右対称・上下対称など、対称性のある配置。安定感・格式・落ち着きを出しやすい
- **アシンメトリー**：左右非対称・上下非対称の配置。動き・変化・個性を出しやすい
- **黄金比**：1：1.618... の比率。自然で美しい比率とされ、デザインや建築などで使われる
- **白銀比**：1：√2 の比率。大和比とも呼ばれ、日本の紙面・建築・デザインでなじみやすい比率
- **GIF形式**：最大256色を扱える画像形式。色数の少ないイラスト・ロゴ・簡単なアニメーションに向く
- **JPEG形式**：写真に向く画像形式。非可逆圧縮なので、圧縮率を高くすると画質が劣化する
- **BMP形式**：Windowsでよく使われる画像形式。基本的に圧縮されず容量が大きく、Web向きではない
- **SVG形式**：ベクター画像形式。拡大縮小しても劣化しにくく、ロゴ・アイコン・図形に向く
- **構造化設計**：システムを機能ごとに分割し、分かりやすく保守しやすい構造に設計する手法
- **コンポーネント**：システムを構成する部品。ソフトウェア部品や機能単位を指すことが多い
- **モジュール**：プログラムを機能ごとに分割した部品。関数、クラス、処理単位など
- **モジュール強度**：1つのモジュール内の処理が、どれだけ1つの目的にまとまっているかを表す度合い。高いほど良い
- **モジュール結合度**：モジュール同士の依存の強さ。低いほど良い
- **機能的強度**：1つのモジュールが1つの機能だけを実現している状態。モジュール強度として最も高く、良い
- **情報的強度**：同じデータ構造を扱う複数の機能を1つのモジュールにまとめた状態。かなり良い
- **連絡的強度**：前の処理結果を次の処理が使うように、処理同士がデータでつながっている状態
- **手順的強度**：処理の順番に関係があるため、複数の処理を1つにまとめた状態
- **時間的強度**：初期化処理や終了処理のように、同じ時期に実行される処理をまとめた状態
- **論理的強度**：似た種類の処理をまとめ、引数などで実行する処理を切り替える状態
- **暗号的強度**：おそらく「偶然的強度」の誤り。偶然的強度は、関係の薄い処理を偶然1つのモジュールにまとめた状態で、最も悪い
- **データ結合**：必要なデータだけを引数として渡す結合。モジュール結合度として最も良い
- **スタンプ結合**：必要な一部だけでなく、レコードや構造体などのまとまったデータを渡す結合
- **制御結合**：他のモジュールの処理内容を制御するためのフラグなどを渡す結合
- **外部結合**：複数のモジュールが外部で定義されたデータや装置に依存している結合
- **共通結合**：複数のモジュールが共通データ領域、グローバル変数などを参照・更新する結合
- **内容結合**：他のモジュールの内部処理や内部データを直接参照・変更する結合。最も悪い
- **デザインレビュー**：設計内容を関係者で確認し、誤りや改善点を見つけるレビュー
- **ラウンドロビン**：参加者が順番に発言・確認する方式。レビューやタスク割当、CPUスケジューリングなどで使われる
- **ウォークスルー**：作成者が資料や設計内容を説明し、参加者が問題点を指摘するレビュー
- **インスペクション**：役割や手順を決めて厳密に行う公式なレビュー。欠陥発見を目的とする
- **モデレータ**：レビューや会議の進行役。インスペクションでは進行管理を担当する

### < 自習 >

- **ハフマン符号化**：出現頻度が高い文字には短い符号、出現頻度が低い文字には長い符号を割り当てて、データ量を小さくする可逆圧縮方式
- **ベクトルコンピュータ**：配列や行列のような大量のデータに対して、同じ演算をまとめて高速に処理するコンピュータ
- **RFIDタグ**：電波を使って非接触で情報を読み書きできるICタグ。商品管理、交通系IC、入退室管理などに使われる
- **MTBF**：Mean Time Between Failures、平均故障間隔。故障してから次に故障するまでの平均時間で、値が大きいほど信頼性が高い

## 明日やること

- 基本情報の学習
- Webクリエイター試験に向けた学習
