# Day99 - Webデザイン基礎理解 & Java基礎理解

## 今日やったこと

- Webクリエイター試験に向けた学習
- Javaの総復習

## 学んだこと

- Javaはコンピュータに命令を書くためのプログラミング言語
- `println`は`print line`の略で「1行表示する」という意味
- `"5"`は文字列、`5`は数値であり意味が異なる
- `+`は数値同士では加算、文字列同士では連結になる
- 変数はデータを入れておく箱のようなもの
- `int`は`integer`の略で整数型
- `String`は文字列型
- `=`は「右の値を左に代入する」という意味
- 変数の値は後から上書きできる
- `x = x + 1`のように、自分自身を利用して値を更新できる
- if文で条件分岐ができる
- elseは「それ以外の場合」を表す
- `==`は値の比較、`=`は代入で意味が異なる
- `true`は条件成立、`false`は条件不成立
- `for`文で繰り返し処理ができる
- `i++`は`i = i + 1`の省略形
- **配列**は複数のデータをまとめて扱う仕組み
- **配列は0番から始まる**
- `numbers[1]`のように番号（インデックス）で要素を取り出す
- メソッドは処理をまとめたもの
- `main`メソッドはJavaが最初に実行する場所
- メソッドには引数を渡せる
- `"Taro"`のように実際に渡す値を実引数という
- `String name`のように受け取る側を仮引数という
- `return`は値を返すために使う
- `void`は値を返さないことを表す
- `class`は設計図のようなもの
- `new`はインスタンス（実体）を新しく作る
- `Person p = new Person();`の`p`はインスタンスを参照する変数
- フィールドはデータ、メソッドは処理を表す
- `p.name`や`p.hello()`の`.`は「〜の」という意味
- コンストラクタはオブジェクト作成時に自動実行される特別な処理
- コンストラクタ名はクラス名と同じになる
- `new Person("Taro", 25)`のように引数付きで初期化できる
- コンストラクタ呼び出し時は、引数の型・順番・個数を一致させる必要がある

## 感想

アルゴリズムやコードの理解を深めるために、Javaを勉強するという位置づけにしている。
基本情報の科目Bのための前準備として、Javaを学習することには大きな意味があると思う。
Bronzeのレベルを理解できるようになることが直近の目標である。

## 明日やること

- 基本情報の科目Aの学習
- Webクリエイター試験に向けた学習
