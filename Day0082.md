# Day82 - IT基礎理解 & Java基礎理解 & AWS基礎理解

## 今日やったこと

### < 訓練校 >

- シフト演算の問題演習
- 論理演算の理解
- Javaの拡張for文の理解

### < 自習 >

- UdemyとPing-tで問題を解いて、AWSの理解を深める

## 学んだこと

### < 訓練校 >

- 論理和（OR）は、どちらか一方、または両方が「真(1)」なら結果は「真(1)」
- 論理積（AND）は、両方が「真(1)」のときだけ結果は「真(1)」
- 否定（NOT）は、入力の真逆を出力する
- 排他的論理和（XOR）は、2つの入力が「異なる」ときだけ結果は「真(1)」
- 否定論理和（NOR）は、ORの結末を反転。両方が「偽(0)」のときだけ結果は「真(1)」
- 否定論理積（NAND）は、ANDの結末を反転。両方が「真(1)」のときだけ結果は「偽(0)」
- 拡張for文の構文は、for (型名 変数名 : コレクション) { forループ内の処理 }
- 拡張for文では、ループ処理のたびに、コレクションの中に入っている要素が1つずつ変数に代入される

### < 自習 >

- Amazon Applocation Recovery Controller（ARC）は、マルチリージョン障害時のトラフィック切替や、切替先リージョンの準備状況チェックを支援するサービス
- 拡張ファンアウトとは、各コンシューマーに専用の読み取りスループットを与える仕組み
- EMRマネージドスケーリングとは、EMRが負荷を見て、クラスターのノード数を自動調整する機能
- RDS Proxyは、接続をプールすることで、フェイルオーバー時の接続回復時間を短縮することもできる
- 

## 感想


## 明日やること

- UdemyとPing-tで問題を解いて、AWSの理解を深める
