# Day134 - IT基礎理解

## 今日やったこと

- 基本情報の学習（科目B対策）

## 学んだこと

- Nodeは、単方向リストの1つの要素を表す箱のようなもの
- Nodeは、「値」と「次のNodeへの参照」を持つ
- この問題では、Nodeクラスのメンバ変数としてkeyとnextがある
- keyは、そのNodeに格納されている値を表す
- nextは、次のNodeへの参照を表す
- node.keyは、「今見ているNodeの値」を表す
- node.nextは、「今見ているNodeの次のNode」を表す
- topは、リストの先頭要素への参照を表す
- nodeは、今見ているNodeを表す作業用の変数
- Node: node ← top は、「先頭のNodeから探索を始める」という意味
- while(nodeが未定義でない) は、「現在見ているNodeが存在する間、処理を続ける」という意味
- nodeが未定義になるのは、リストの末尾を超えて、次のNodeが存在しなくなったとき
- search(文字型: key) のkeyは、「探したい値」を表す
- node.keyのkeyは、「Nodeの中に入っている値」を表す
- node.key が key と等しい は、「今見ているNodeの値が、探したい値と同じか」を判定している
- 一致すれば、return "探索成功" で関数を終了する
- 一致しなければ、次のNodeへ進む必要がある
- 次のNodeへ進む処理は、node ← node.next
- node ← node.next は、「現在見ているNodeを、その次のNodeに更新する」という意味
- node ← node.next がないと、同じNodeを見続けてしまい、無限ループになる
- リストを最後までたどっても一致するkeyが見つからなければ、return "探索失敗" になる
- この問題の空欄には、node ← node.next が入る
- 配列で表す単方向リストでは、A[i,2] が「次の要素」を表すことが多い
- 配列版で次へ進む処理は、i ← A[i,2]
- Node版で次へ進む処理は、node ← node.next
- 配列版のA[i,2]と、Node版のnode.nextは、どちらも「次の要素への参照」という意味
- 単方向リストの探索は、先頭から順番にnextをたどって、目的の値を探す処理
- Node版の単方向リストでは、「行番号」ではなく「Nodeへの参照」を使って次の要素をたどる
- top → Node1 → Node2 → Node3 のように、topからnextをたどってリスト全体を読む
- key、next、top、nodeは予約語ではなく、問題文で定義された変数名
- Nodeやnextという言葉が出てきたら、「箱」と「次の箱への矢印」と考えると分かりやすい

## 感想

やはり1問あたり5分のペースで解かないといけないというのがネックになっている。
全問正解する必要はないのだが、できるだけ多くの問題を解けたほうが良いので、ペース配分はやはり重要である。
プログラムの流れを追うのにも時間がかかるが、問題文を読んで何をしているプログラムで、問題となっているのはどの部分なのかを理解するのに時間がかかっている。
先に選択肢を見るようにするなど、工夫はしているが、それでも時間が足りていない。
本番の問題が、サービス問題と言えるような短時間で解ける問題が何問含まれるのかでも、大きく違う。
実際の問題レベルがわからないので、一向に自信はつかない。

## 明日やること

- 基本情報の学習（科目B対策）
