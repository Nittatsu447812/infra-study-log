# Day70 - AWS基礎理解

## 今日やったこと

- 構造理解を意識しながらUdemyの模擬試験を解いて、AWSの理解を深める

## 学んだこと

- DBもスケールする場合はAmazon Aurora Serverlessを使う
- オンライン・トランザクション処理（OLTP）は、リアルタイムで細かい更新が大量に発生する処理
- AWS Organizationsは、複数アカウントをまとめて管理する仕組み
- OU（Organization Unit）は、アカウントをグループ化する箱で、求めてルールを適用するために使う
- １つのアカウントは１つのOUにしか所属できない（Linuxのプライマリグループに近いイメージ）
- SCP（Service Control Policy）は、アカウントに対する"最大権限の制限"
- 最終的な権限は、SCP（上限）とIAM（許可）の両方が必要であり、どちらかがNGなら実行できない
- ALBにおいて、リスナー（Listener）はポートごとの受付窓口（入口）
- リスナールールは、その入口に来た通信をどう処理するか（転送（forward）、リダイレクト（redirect）、固定レスポンス）
- ターゲットグループは、実際の転送先（EC2など）
- Amazon S3 Storage Lensは、S3の利用状況を可視化・分析する（S3の健康診断ツール）
- IAM Access Analyzer for S3は、S3の公開範囲の設定チェックができる（公開事故検知ツール）
- Amazon S3 Block Public Accessは、S3の公開設定を無効化・無視する（絶対公開させないためのガード）
- EC2にパブリック名が付く条件は、①VPC側：DNS解決（enableDnsSupport）とDNSホスト名（enableDnsHostnames）がON　②インスタンス側：パブリックIPを持っている
- ボールト（Vault）は、S3 Glacierの中でのデータを入れる箱（S3でいうバケット）
- "ポリシー変更不可"というワードがあれば、"Lock"を探す
- DataPipelineは、現在レガシーサービスであり、置き換えには同じ役割を持つGlueを使うことが多い
- プロファイル認証は、EC2にIAMロールをアタッチする仕組み
- IAMデータベース認証は、RDSの機能でIAMでDBログインできる仕組み
- BYOIP（Bring Your Own IP）は、自分が元々持っているIPをAWSに持ち込む仕組み
- ROA（Route Origin Authorization）は、「このIPをAWSから発信してOK」という許可証
- RAID0は複数ディスクを束ねて性能（IOPS）を上げる
- RAID1は同じデータを2つコピーし（ミラーリング）、耐障害性を上げるが、性能（IOPS）は上がらない
- IOPS（Input/Output Operations Per Second）は「1秒間にディスクが何回読み書きできるか」
- EBSの代表的な種類は、①gp3（バランス型、安い、そこそこ速い）、②io1/io2（高性能、IOPSを指定できる、コストも高い）、③io2 Block Express（最強クラス、超高IPOS、超高コスト）
- AWS IAM Identify Center（旧名：SSO）は、AWSの統合ログインサービス
- Active Drectoryは社内のユーザー・パスワード・権限を一括管理する"認証の中心サーバ"
- 拡張モニタリング（RDS Enhanced Monitoring）は「DBの中の動き（OSレベル）」を監視できる
- スロットリングは、サーバ保護やコスト爆発防止の観点から、アクセス数をあえて制限する仕組み
- 署名付きURLは、URL単位でのアクセス制御（単一ファイルごとの署名で、URLが変わる）
- 署名付きCookieは、Cookieにアクセス権をもたせる方式（一度ログインすると複数ファイルOK、URLはそのまま使える、動画配信サイト向け）

## 感想

正答率は徐々に上がっている。自信を持って答えられる問題も増えてきている。しかし、問題をとき進めていると初めて聞くサービス名や用語が登場する。100%理解することは難しいし、SAAの合格に100%の理解は必要ない。それを目指し始めるとキリがない。
正答率がさらに伸びて80~85％くらいで安定したら本番試験を受けに行こうと思う。目標は５月中の受験である。

## 明日やること

- AWSの学習を進める
