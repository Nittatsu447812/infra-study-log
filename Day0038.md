# Day38 - Linux基礎理解

## 今日やったこと

- 102試験の範囲の学習(2周目)
- Ping-tで問題演習と分からなかったところの復習

## 学んだこと

- シェル関数の引数は`$1`、`$2`、`$3`などでアクセスでき、存在しない引数は空行になる
- $*は全引数(1つにまとめる)を表す引数で、区切り文字は環境変数IFSで定義される
- $?は直前のコマンドの戻り値を表す
- execコマンドは現在のプロセスを置き換える
- "-eq は equal"、"-ge は greater or equal"、"-gt は greater than"
- -fは「最終的に参照される実体が通常ファイルか」を判定する
- -eは「実体ファイルが存在するか」を判定する(ファイルかディレクトリかは関係ないが、シンボリックリンクの場合はリンク先が存在している必要がある)
- -Lは「シンボリックリンクかどうか」を判定する(リンク先が存在するかは関係ない)
- "[ 条件式 ] && 実行文"は条件が満たされる(戻り値が0)なら実行文を実行する
- "[ 条件式 ] || 実行文"は条件が満たされない(戻り値が0以外)なら実行文を実行する
- seqコマンドは負の間隔の場合、終了値を下回ると出力終了
- seq 10 -2 -1を実行すると出力は、10 8 6 4 2 0になる
- readコマンドは標準入力から1行読み取り、指定した変数に格納する

## 感想

今日はエイリアス、シェル関数、シェルスクリプトを中心に2周目の学習のを進めた。最初の方にやった範囲であるほど忘れていることが多い。
しかし、この範囲の理解度は8割近くになったと思う。有意義な復習時間にできた。

## 明日やること

- 102試験の範囲の学習(2周目)
- Ping-tで問題演習と分からなかったところの復習
