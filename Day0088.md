# Day88 - Webデザイン基礎理解 & Java基礎理解 & IT基礎理解 & AWS基礎理解

## 今日やったこと

### < 訓練校 >

- Webページ作成練習
- Javaにおけるメソッドの基礎理解
- クライアントサーバシステム
- RAIDと信頼性設計
- 行列の理解
- 確率・統計の理解

### < 自習 >

- UdemyとPing-tで問題演習を解いて、AWSの理解を深める

## 学んだこと

### < 訓練校 >

- `<figure>`は、画像やグラフ、コードなど「ひとまとまりの図表コンテンツ」を表す枠組み
- `<figcaption>`は、その図表に対する「タイトルや説明文（キャプション）」
- メソッド（Method）とは、特定の処理（プログラム）をひとまとめにした「部品」のこと
- メソッドを使うことで、同じコードを何度も書かなくて済む（再利用性）、プログラムが見やすくなる（可読性）、修正やエラーの発見が楽になる（保守性）というメリットがある
- 引数（ひきすう）とは、メソッドを呼び出すときに、処理に使うデータ（値）を外部から渡すための仕組み
- mainメソッドの引数は、コマンドラインから値を渡すことができる
- ストアドプロシージャ（Stored Procedure）とは、データベース（RDB）に複数のSQL処理をひとまとめにして保存（ストア）したプログラムのこと
- 仮想化の形態には、ホスト型、ハイパバイザ型、コンテナ型の3種類がある
- RAID 0（ストライピング）は、データを細かく分解し、複数のディスクに同時にバラバラに書き込む仕組み
- RAID 1（ミラーリング）は、2台以上のディスクに、全く同じデータを同時に書き込む仕組み
- RAID 5（パリティ分散）データと一緒に、「パリティ」と呼ばれる復旧用のデータを計算し、すべてのディスクに分散して書き込む仕組み

### < 自習 >

- DataSyncは、データ移行・同期する
- Storage Gatewayは、オンプレからAWSストレージを使う仕組み
- RDS Customは、「OS/DBに深く触れるようにしたRDS」
- S3イベント通知は、同一条件を複数宛先へ直接通知しづらい（だからSNSでファンアウトする）
- Gateway Load Balancer（GWLB）は、通信をFirewall/IDSへ透過的に流すためのLB

## 感想

Ping-tの模擬試験モードで初見の問題に絞って解いてみたところ、49問/65問で正答率75%だった。
余裕のある数字ではないが、一応合格ラインは超えられていると思う。
未知のサービスや機能名が問題文に登場すると、頭が真っ白になることがあるので、ある程度割り切ってあきらめて次の問題に進むようにしたほうが良い。ただし、未知のサービス名が登場したからと言って、今ある知識で解けないとは限らないので、問題文と選択肢はしっかり読み込むようにしなければならない。

## 明日やること

- UdemyとPing-tで問題演習を解いて、AWSの理解を深める
