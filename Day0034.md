# Day34 - Linux基礎理解

## 今日やったこと

- 102試験の範囲の学習(1周目)

## 詰まったこと

- predictable namingの必要性
- 現在は非推奨(deprecated)とされるコマンドとその後継のコマンドの組み合わせ

## 学んだこと

- pingコマンドはICMP Echo Request/Replyを使って指定したホストの疎通確認ができる
- tracerouteコマンドはTTLを段階的に増やして経路を特定できる
- tracepathコマンドはtracerouteの簡易版で、MTU(最大転送単位)も確認できる
- ping6/traceroute6/tracepath6は古い書き方で、現在はping -6/traceroute -6/tracepath -6で代替されることが多い
- hostnameコマンドはホスト名の表示・設定ができるが、名前の設定にはroot権限が必要(一時的変更)
- 現在はhostnamectl set-hostnameでホスト名の設定を行うのが主流(永続的変更)
- deprecated(非推奨)とは「今は使えるが将来的には可能性があり、新しい手法を使うべき」という意味
- netstatコマンドは非推奨で、その代替はssコマンド
- NIC(Network Interface Card)は物理デバイス、ネットワークインターフェイスはOS上の論理的な通信口(eth0、wlan0など)
- ソケットは通信の出入口で、「IPアドレス + ポート番号 + プロトコル」で識別される
- ncコマンドで任意ポートへの接続・通信テストができる
- routeコマンドでルーティングテーブルの表示・設定ができる(非推奨、ip routeコマンドで代替)
- ルーティングは宛先IPに基づいてパケットの経路を決定する仕組み
- ルーティングテーブルとは宛先・ゲートウェイ・インターフェースなどの対応表
- ipコマンド現代の標準ネットワーク管理コマンド
- ifconfigコマンドはインターフェースの表示・設定ができる(非推奨、ip linkコマンドで代替)
- eth0は「1枚目の有線LAN」のネットワークインターフェイス名
- wlan0は「1枚目のWi-Fi」のネットワークインターフェイス名
- loは「ループバックインターフェイス」
- ループバックインターフェイスは自分自身と通信するための特別なインターフェイス
- predictable namingとは「物理的な位置や情報に基づいて名前を固定する仕組み」
- DNSはインターネット全体で使われている、名前解決を行う仕組み
- 名前解決はホスト名をIPアドレスに変換すること(正引き)
- /etc/nsswitch.confは名前解決の順番を定義
- /etc/resolv.confでDNSサーバを指定する
- /etc/systemd/resolved.confはsystemd-resolvedの設定ファイル
- hostコマンドはシンプルなDNS問い合わせツール
- digコマンドで詳細なDNS情報を取得できる(トラブルシュートで重要)

## 感想

一連の流れを意識して学習できた。今日でネットワークの基礎を1周できた。まだ理解が足りないところは多いだろうが、2周目以降で更に理解を深めたい。
明日はもう1つの重要な山場であるセキュリティの範囲を学習する。

## 明日やること

- 102試験の範囲の学習(1周目)
