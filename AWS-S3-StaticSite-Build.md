# AWS : S3静的ウェブサイトホスティングによるサーバレスWeb公開の検証

## 目的

AWSのストレージサービスであるS3の「静的ウェブサイトホスティング」機能を利用し、サーバ（OS）の管理・構築を行わずにWebページを迅速かつ低コストで公開する手順を習得する。

## 構成

- **クライアント環境**: Ubuntu 24.04 LTS（ローカルPC）
- **クラウド環境**: AWS（S3）
- **リージョン**: 東京（ap-northeast-1）
- **サービス名**: Amazon S3
- **バケット名**: test-bucket-0419913
- **公開ファイル名**: index.html

## 構成図

<img width="690" height="235" alt="AWS-S3-StaticSite20260419 drawio" src="https://github.com/user-attachments/assets/edf99e33-02b3-490a-a285-dba25fe8b148" />

## 実施内容と検証

### 1. S3バケットの作成と設定

- **バケットの作成**: リージョンを東京に指定し、一意のバケット名で作成。
- **パブリックアクセスの許可**: デフォルトの「パブリックアクセスをすべてブロック」を解除し、外部からの閲覧を可能に設定。
- **静的ウェブサイトホスティングの有効化**: インデックスドキュメントにindex.htmlを指定し、ウェブサイトとしてのエンドポイント（URL）を生成。

### 2. コンテンツのアップロード

- **HTML作成**: ローカルのUbuntu環境で、`<h1>`と`<p>`のみを使った簡易的なindex.htmlを作成。
- **アップロード**: S3バケットのルートディレクトリにファイルを配置。

### 3. バケットポリシーによる権限管理

- **ポリシーの適応**: JSON形式のバケットポリシーを記述し、バケット内のオブジェクトに対してs3:GetObject権限を匿名ユーザーに許可。
- **動作確認**: 生成されたエンドポイントにブラウザからアクセスし、正常にページが表示されるかを確認。

### 4. 修正（トラブルシューティング）

- **初期検証**: 当初、`<h1>`タグと`<p>`タグのみで記述した簡易的なindex.htmlをアップロードしたが、ブラウザでアクセスした際に日本語が文字化けする現象が発生。
- **原因調査**: ブラウザがファイルのの文字エンコーディング正しく識別できていないことが原因だと判明。
- **修正対応**: HTMLの基本構造（`<html>`、`<head>`、`<body>`）を整え、`<head>`内に`<meta charset="UTF-8">`を明示的に追加。これにより、ブラウザがUTF−8として正しく解釈し、文字化けが解消されることを確認。


<img width="1706" height="162" alt="Screenshot 2026-04-19 at 17-42-42 Screenshot from 2026-04-19 17-25-15 png (PNG 画像 1854 × 1048 px) — 表示倍率 (91%)" src="https://github.com/user-attachments/assets/8d99c129-7d78-4a72-ad47-4648c89b8d03" />

<img width="1705" height="160" alt="Screenshot 2026-04-19 at 17-46-45 Screenshot from 2026-04-19 17-45-40 png (PNG 画像 1854 × 1048 px) — 表示倍率 (91%)" src="https://github.com/user-attachments/assets/d30d6d36-511c-48f1-a1dd-575aff8dee15" />

## 課題と学び

- **サーバレスの利便性**: EC2のようなインスタンス管理やミドルウェア設定が不要で、コンテンツ配信に特化できる利点を実感。
- **Web標準の重要性**: クラウド側の設定（S3）だけでなく、提供するコンテンツ（HTML）側で適切なメタ情報を定義することの重要性を再認識。
- **権限管理の多重性**: S3のブロック設定解除とバケットポリシーの両方が必要であることを実機操作を通して理解。
