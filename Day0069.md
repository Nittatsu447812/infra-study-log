# Day69 - HTML基礎理解 & CSS基礎理解 & IT基礎理解 & AWS基礎理解

## 今日やったこと

### < 訓練校 >

- Webデザインの基礎理解
- 浮動小数点の理解
- 

### < 自習 >

- 構成を意識しながらUdemyの模擬試験を解いて、AWSの理解を深める

## 学んだこと

### < 訓練校 >

- floatプロパティを適用された要素はその親要素のコンテンツボックスの「限りなく左上（または右上）に配置される」（`float: left;`）
- overflowプロパティでフロートを解除できる（`overflow: hidden;`）
- overflowプロパティを書く場所は1つ上の階層
- `F12キー`でブラウザ（Chrome）の開発者ツールの利用ができる
- ブロックレベルボックスは1つの塊として扱われ、前後に必ず改行が入る
- ブロックレベルボックスは指定しなくても、横幅めいっぱいに広がる
- ブロックレベルボックスはwidth（幅）やheight（高さ）を指定できる
- インラインレベルボックスは文章の一部として扱われるため、改行されず、横に並んでいく
- インラインレベルボックスは中身（文字など）の大きさに合わせられるため、CSSで幅や高さを指定しても効かない
- `display: block;`でインラインレベルボックスをブロックレベルボックスに変えられる
- 浮動小数点は小数部分を含む実数を扱う場合に使用する形式で、指数を使うことで、大きな数や小さな数を固定小数点よりも少ないビット数で表現できる
- 
### < 自習 >

- Iotストリーミングの王道パターンは、収集、リアルタイム処理、変換、安価ストレージ保存の流れである
- Firehose単体では柔軟な変換は弱いため、Lambdaで変換するのが王道
- Amazon Maneged Service for Apache Flinkは「流れてくるデータをリアルタイムで計算・分析するエンジン」（データの"意味"を作る）
- 変換（Transformation）は「保存しやすい形、分析しやすい形にデータのフォーマットを変える」（主にLambda＋Kinesis Data Firehoseが担う）
- 時間が決まっている負荷増加に対する解決策は「スケジュールドスケーリング」
- ECSには、EC2起動タイプ（自分でサーバを用意する）、Fargate（サーバ不要）の2種類ある
- ECSとALBを組み合わせるとき、ALBはコンテナ単位で振り分ける
- キャパシティプロバイダー（Amazon ECS Cpacity Provider）は、「ECSとASG（Auto Scaling Group）をつなぐ"橋渡し役"」
- クロスリージョンレプリケーション（CRR）は、S3の機能で、「別リージョンのS3バケットに自動コピーする仕組み」（災害対策で使う）
- CRRは非同期で自動コピーを行うが、既存データはコピーされず、新しいオブジェクトのみコピーされ、その方向は片方向である
- ロギングは、「誰が・いつ・どのキーを使ったかの記録」

## 感想


## 明日やること

- 構成を意識しながら、AWSの学習を進める
